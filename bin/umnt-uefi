#!/bin/bash

if [[ $# -eq 1 ]]; then
    mnt=$1
else
    echo "umnt-uefi <mnt-point>" >&2
    exit 1
fi

if [[ ! -d $mnt ]]; then
    echo "Missing <mnt> directory ($mnt)" >&2
    exit 1
fi


sudo umount $mnt
sudo losetup -d /dev/loop0

echo "Unmounted ($mnt)"
