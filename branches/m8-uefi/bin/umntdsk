#!/bin/bash

echo "umount /mnt"
sudo umount mnt

echo "qemu-img convert -O vmdk vm/disk1.img vm/disk1.vmdk"
qemu-img convert -O vmdk vm/disk1.img vm/disk1.vmdk

echo "sudo losetup -d /dev/loop0"
sudo losetup -d /dev/loop0
